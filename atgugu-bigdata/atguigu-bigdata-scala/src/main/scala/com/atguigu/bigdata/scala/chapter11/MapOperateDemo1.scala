package com.atguigu.bigdata.scala.chapter11

object MapOperateDemo1 {
  /*
 请将List(3,5,7) 中的所有元素都 * 2 ，
 将其结果放到一个新的集合中返回，即返回一个新的List(6,10,14), 请编写程序实现.

  */
  def main(args: Array[String]): Unit = {
    //传统方法
    /*val list = List(3, 5, 7)
    var list2 = List[Int]()
    for (i <- list) {
      list2 = list2 :+ i * 2
    }
    println(list2)*/
    //使用高阶函数
    val list = List(3, 5, 7)
    val res = test(mult2 _,3.5)
    println(res)
  }

  //说明
  //1. test就是一个高阶函数
  //2. f: Double => Double 表示一个函数， 该函数可以接受一个Double,返回Double
  //3. n1: Double 普通参数
  //4. f(n1) 在test函数中，执行 你传入的函数
  def test(f: Double => Double, n1: Double) = {
    f(n1)
  }

  def mult2(d: Double): Double = {
    d + d
  }
}




























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































